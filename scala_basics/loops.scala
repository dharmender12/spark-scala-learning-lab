object Loops {
    def main(args: Array[String]): Unit = {
    // var a = 6;
    // while (a > 0 ){
    // println("The value is :"+a)
    // a = a -1;
    for (a <- 1 to 5) {
        println(a)
        
    }

}
}